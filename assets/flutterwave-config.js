/* ND Electronic Technologies Ltd — Flutterwave payment config
   Fill in after creating a Flutterwave account (see SETUP.md).
   Dashboard → Settings → API Keys → "Public Key" (starts with FLWPUBK_).
   Never put your SECRET key in this file — it belongs only in the
   Supabase Edge Function's environment variables (server-side). */

const FLW_PUBLIC_KEY = 'FLWPUBK-YOUR-PUBLIC-KEY-HERE';
